{
  "name": "draftharbour-desktop",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "build:mac": "tauri build --target universal-apple-darwin",
    "build:mac:debug": "tauri build --debug --target universal-apple-darwin"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.3"
  }
}
