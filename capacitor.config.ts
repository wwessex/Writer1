import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.draftharbour.studio',
  appName: 'DraftHarbour Studio',
  webDir: 'dist'
};

export default config;
